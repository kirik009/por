import { combineReducers } from 'redux'
import dataUsers from './todos'
import users from './visibilityFilter'

const rootReducer = combineReducers({
  dataUsers,
  users
})

export default rootReducer