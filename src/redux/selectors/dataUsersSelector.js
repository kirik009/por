// import { createSelector } from 'reselect'

// const getUserss = state => state.users
// export const getUsers = createSelector(
//     [getUserss],
//     users => (
//       users.reduce((count, todo) =>
//         todo.completed ? count + 1 : count,
//         0
//       )
//     )
// )